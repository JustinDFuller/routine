# Dogfooding Checklist

Use this checklist before relying on the MVP on a simulator or personal iPhone.

- Install and launch the app from a clean build.
- Confirm the launcher icon is visible and recognizable on the Home Screen.
- Confirm the launcher icon is visible in the App Library.
- Verify starter data appears on first launch.
- Force-quit and relaunch the app, then confirm the existing data persists.
- Complete a routine from the dashboard and confirm the card updates immediately.
- Attempt a duplicate same-day completion and confirm it behaves as a no-op.
- Undo a same-day completion and confirm the dashboard returns to the prior state.
- Open routine history and remove a historical completion.
- Confirm the history screen and dashboard reflect the correction after returning.
- Add a new routine.
- Edit an existing routine.
- Delete a routine and confirm its history is removed.
- Add a new group.
- Rename a group.
- Reorder routines within a group.
- Move a routine into a different group.
- Reorder groups.
- Accept the behind-schedule alert onboarding prompt and confirm the alert setting turns on at the default 7:00 AM.
- Relaunch after enabling alerts and confirm the selected notification time persists.
- Deny notification permission and confirm the app remains usable without broken settings or alerts.
- After denial, open iOS Settings, choose Routine, enable notifications, then verify scheduling resumes without duplicate requests.
- Set week start to Sunday; before 7:00 AM on Thursday, create a weekly target of three with one completion; ensure every other routine is on pace or removed; then verify exactly two pending requests for the chosen routine—the Thursday and Friday occurrences—contain its projected deficit.
- Complete, edit, or remove the routine, foreground again, and confirm stale pending requests are replaced.
- Add the widget to the Home Screen and confirm the small layout renders with a routine or empty state.
- Add the medium widget and confirm the routine name, count, period, and last-done text render legibly.
- Confirm the widget shows the "Add routines in Routine" state when the store is empty.
- Confirm the widget shows the "All caught up" state when everything available is already complete.
- Complete a routine from the widget and confirm the app opens back on Today with the completion banner restored.
- Tap the widget body and confirm it routes to Today rather than an unrelated screen.
- Remove and re-add the widget, then confirm it repopulates from the shared store.
- Verify the app remains usable in both dark mode and light mode.
- Verify the app remains usable with larger Dynamic Type sizes.
- Verify VoiceOver can reach the primary dashboard, history, and management flows.
- Verify Reduce Motion does not introduce broken transitions or blocked flows.
- Verify the primary dashboard interactions remain comfortable for one-handed use on iPhone.
- Run the same checklist on at least one secondary internal TestFlight device before widening the tester list.
